/**
 * Costanti condivise tra server (pagina index, action) e client
 * (GalleryMosaic) per la pagina /news/gallery. File "neutro" — niente
 * "use client" o "use server", import safe ovunque.
 */
export const GALLERY_PAGE_SIZE = 20;
